<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="@color/white"
    android:orientation="vertical">

    <FrameLayout
        android:id="@+id/video_fl"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@drawable/bg_video_gridview">

        <ImageView
            android:id="@+id/video_img"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:adjustViewBounds="true"
            android:background="@drawable/bg_video_gridview"
            android:scaleType="fitXY"
            android:src="@drawable/ic_loadings" />

        <ImageView

            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:scaleType="fitCenter"
            android:src="@drawable/ease_video_play" />

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_gravity="bottom"
            android:background="@color/default_loading_color"
            android:orientation="horizontal">

            <TextView
                android:id="@+id/video_size_text"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:padding="@dimen/default_padding_and_margin"
                android:text="0KB"
                android:textColor="@color/white"
                android:textSize="@dimen/default_x_text_size" />

            <TextView
                android:id="@+id/video_time_text"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:gravity="right"
                android:padding="@dimen/default_padding_and_margin"
                android:text="00:00"
                android:textColor="@color/white"
                android:textSize="@dimen/default_x_text_size" />
        </LinearLayout>

    </FrameLayout>

</LinearLayout>